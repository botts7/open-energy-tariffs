# Architecture & decisions (open-energy-tariffs)

> **This document is the bulk of the project.** Each section is a decision to be
> made *before* writing importers or consumers. Fill with: the decision, the
> options considered, and the rationale (treat as lightweight ADRs). Status one
> of: `OPEN` / `DECIDED` / `DEFERRED`.

---

## 1. Canonical model vs app-specific model — `OPEN`
The seed `schema/tariff.schema.json` mirrors the Wallbox add-on's tariff shape.
Decide whether the stored model is a **neutral canonical** representation with
per-app **adapters**, or stays app-specific.
- Options: (a) neutral canonical + adapters (Wallbox, generic HA, raw); (b)
  Wallbox-shaped now, refactor later; (c) two layers — canonical store + a
  `dist/` compiled per-app.
- Decision: …
- Rationale: …

## 2. Scope & boundaries for v1 — `OPEN`
- Countries/sources in v1: …
- STATIC presets only? How DYNAMIC (live-price) providers are documented/handed
  off (consume from HA price entity, not stored): …
- Explicitly out of scope: …

## 3. Plan identity, dedup & time-versioning — `OPEN`
- Unique key for a plan (country/region/network/provider/plan/?): …
- How rate **changes over time** are modelled — effective-date ranges (mirror the
  add-on's tariff-history `validFrom`)? One file per version, or a `history[]`?: …
- Multiple regions/networks sharing one plan: …

## 4. Schema governance & extensions — `OPEN`
- Schema versioning + non-breaking evolution: …
- Missing structures to add (and whether v1 needs them): tiered/block rates,
  demand charges, controlled load, **solar feed-in (export) tariff**, daily
  supply charge, EV-only windows: …

## 5. Distribution architecture — `OPEN`
- How consumers pull: GitHub raw / Pages / CDN / npm package / GitHub Release
  asset?: …
- Single `dist/tariffs.json` vs per-country chunks (size/scale): …
- Versioning + pinning; cache / ETag; offline bundled-snapshot fallback: …

## 6. Licensing & legal — `OPEN` (needs real research)
- Per-source redistribution rights: **OpenEI URDB**, **AU-CDR**, **Octopus** — may
  we bulk-store, or import on-device only?: …
- Repo licence (code MIT; data CC0?) + attribution requirements: …
- Per-source decision: bulk-store vs on-device-import: …

## 7. Trust / quality model — `OPEN`
- `verified` vs unverified; who can mark verified; staleness policy
  (`updated` age): …
- Moderation + conflict resolution between submissions: …

## 8. Importer architecture — `OPEN`
- On-device vs build-time imports per source: …
- Mapping each source's model → schema; drift handling; API keys/rate limits: …

## 9. Contribution & privacy UX — `OPEN`
- PR flow + template; in-app "submit my plan" → pre-filled PR vs JSON export: …
- CI checks for personal data (NMI/MPAN/meter/address patterns): …

## 10. Consumer SDK — `OPEN`
- Tiny JS/Python helper (load → pick → apply/map-to-app) vs raw JSON consumption: …

## 11. Naming / ownership / governance — `OPEN`
- Final repo name + owner/org (schema `$id` currently `OWNER`): …
- Maintainership + contribution governance: …

---

### Output of Phase 0
When these are `DECIDED`, the rest is mechanical: finalise the schema, add CI,
write importers (AU-CDR first), wire the add-on consumer. See `NEXT-STEPS.md`.
